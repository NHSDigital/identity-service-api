[pytest]
addopts = --strict-markers -v
python_classes = Test*Suite
junit_family=xunit1
markers =
    errors: check against error conditions
    happy_path: this is a description
    token_endpoint:
    authorize_endpoint:
    token_refresh:
